import React from 'react';
import { Login } from '../components'

const LoginPage = () => {
    return (
        <section>
            <Login />
        </section>
    );
}

export default LoginPage;
